@model ErrorViewModel
@{ ViewData["Title"] = "Có lỗi xảy ra"; }
<section class="surface detail-panel"><p class="eyebrow">KHÔNG THỂ HOÀN THÀNH</p><h1>Có lỗi xảy ra</h1><p>Vui lòng thử lại. Nếu lỗi tiếp tục, hãy kiểm tra kết nối cơ sở dữ liệu.</p>@if (Model.ShowRequestId) { <p class="text-muted">Mã yêu cầu: <code>@Model.RequestId</code></p> }<a class="btn btn-primary" asp-controller="Home" asp-action="Index">Về trang tổng quan</a></section>
